from tools.httpx_tool import run_httpx as execute_httpx


def run_httpx(subdomains: list):
    """
    Executes HTTPX.

    Returns standardized results.
    """

    return execute_httpx(subdomains)
